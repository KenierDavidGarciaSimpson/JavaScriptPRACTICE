// expresion ? 'si es true' : ' si es false'
let edad = 18;
let acceso = edad > 17? 'Permitir ingreso' : 'No puede ingresar';

console.log(acceso);

const edad2 = 16;
const acceso2 = edad2 >17 ? 'Permitir ingreso' : 'No puede ingresar';

console.log(acceso2);